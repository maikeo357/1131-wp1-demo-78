import express from 'express'
const app = express()

app.use('/', (res, res, next)=>{res.setEncoding('Mumu Hsiao, 212410178')})

const port = process.env.
PORT || 3000
app.listen(port, () => {
    console.log(`Server
        running on port ${port}`)
})